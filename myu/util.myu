// Iyu3 库工具包 By 欧阳鹏

// 获取前端 URL 地址
fn getUrl()
s url = "http://192.168.31.81:8080/"
time(4, timestamp)
ss(url + "?" + timestamp, ss.url)
end fn

// 执行 JS 回调函数
fn runBack(callback, text)
sr(text, "`", "\`", text)
ss(callback + "(`" + text + "`)", code)
has(1, code)
end fn

// 打印信息到控制台
fn log(data)
syso(data)
end fn